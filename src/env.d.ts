/** Injected at build time by scripts/build.mjs (esbuild define). */
declare const __GOOGLE_CLIENT_ID__: string;
/** Realtime game-server URL (co-op), injected at build time. */
declare const __WS_URL__: string;
